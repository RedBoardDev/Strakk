---
description: "DESIGN.md is the source of truth for all UI decisions"
paths:
  - "iosApp/**/*.swift"
  - "androidApp/**/*.kt"
---
# Design System

- `DESIGN.md` is the source of truth. Use its token vocabulary.
- Personality: supportive coach, premium dark, not gym-bro neon. Data is the hero.
- Design for gym context: one-handed use, large touch targets, dim lighting.
- iOS = Apple-native. Android = Material-native. Architecture stays aligned.

See `strakk-design-system` skill for full guidelines.
