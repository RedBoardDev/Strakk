# Supabase project (client-side config — the anon key is public by design,
# security is enforced by RLS server-side).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
