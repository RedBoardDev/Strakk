storage:
  performance:
    max_search_threads: 0  # auto-detect

service:
  grpc_port: 6334
  enable_tls: false

# HNSW index config (applied at collection creation, not here — this is for reference)
# hnsw:
#   m: 16
#   ef_construct: 200
